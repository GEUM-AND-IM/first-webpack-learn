const logInput = (input) => `입력받은 값 : ${input}`;
const logResult = (figure, result) => `${figure} 넓이 : ${result}`;
const logFigureError = "지원되지 않는 도형입니다.";

module.exports = {
  logInput,
  logResult,
  logFigureError,
};
